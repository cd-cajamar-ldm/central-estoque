/* Sobrescrito no deploy do GitHub Pages (.github/workflows/pages.yml) com o
   commit e o horário reais do build. Os valores abaixo só aparecem rodando
   local (http.server, sem passar pelo deploy) — nunca em produção. */
window.MD_BUILD = { commit: 'local', data: '—' };
